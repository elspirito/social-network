import React from 'react'
import styled from 'styled-components'

export const PageTitle = () => {
  return <StyledPageTitle>Page Title</StyledPageTitle>
}

const StyledPageTitle = styled.h1`
  font-size: 20px;
  font-weight: bold;
`
